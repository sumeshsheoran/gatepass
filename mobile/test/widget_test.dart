import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // Placeholder — app uses Riverpod + GoRouter which require full integration setup.
  });
}
